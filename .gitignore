# Build and tool state
_build/
_opam/
*.install
.sinter/
bridge/target/

# What a pass of the mutation tester writes at the root of the
# project: mutaml-runner's working directory, which also holds the
# report files that the documented commands and the mutation job
# write, and the report that the runner writes beside it.
_mutations/
mutaml-report.json

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
Icon?

# Claude Code agent worktrees
.claude/worktrees/

# Local instructions for one person, loaded by Claude Code
CLAUDE.local.md
